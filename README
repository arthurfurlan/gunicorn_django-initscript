Initscript for gunicorn + django.

 1. Save the file under the same structure of this repository:
    - /etc/init.d/gunicorn-django
    - /etc/default/gunicorn-django

 2. Configure the file "/etc/default/gunicorn-django".

 3. Start the daemon:
    - /etc/init.d/gunicorn-django start
